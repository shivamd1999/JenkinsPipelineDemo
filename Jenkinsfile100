pipeline{
    agent any
    
    stages{
        stage("Build number "){
        options { timestamps ()}
            steps{
                echo "The build number is ${env.BUILD_NUMBER}, ${currentBuild.durationString.minus('and counting')}"
            }
        }
    }     
}
