pipeline {
    agent any
    stages {
        stage('Setup parameters') {
            steps {
                script {
                    properties([gitLabConnection(gitLabConnection: '', jobCredentialId: ''), parameters([choice(choices: ['one', 'two ', 'three'], description: '''Job_Name
                    Build_Number Build_url''', name: 'Task_1')])])
                }
            }
        }
    }   
}
